import Spinner from 'components/ui/Spinner'
import { StyledDiv } from 'styles/components/Loading'

const Loading = () => (
    <StyledDiv>
        <Spinner />
    </StyledDiv>
)

export default Loading
